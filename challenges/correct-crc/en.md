# The Correct CRC

This should be really easy, right?

You're given a ZIP archive. Getting inside is only the first step — the archive has more than one trick waiting for you. Follow the clues to the flag.

The flag format for this challenge is `byuctf{8_hex_characters}`.
