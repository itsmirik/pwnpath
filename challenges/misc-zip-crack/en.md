# Guessy

How good are you at password cracking?

You're given a password-protected ZIP archive (`secret.zip`). Crack the password,
open the archive, and the flag is waiting inside.

Flag format: `ctf{...}`
