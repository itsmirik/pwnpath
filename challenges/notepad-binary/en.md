Here is a compiled 64-bit Linux program. When run it prompts for a flag and tells you whether you are `Correct!` or `Wrong :(`.

You do not need to understand assembly for this one. The flag is compared against a hard-coded string that is sitting in plain sight inside the file. Reach for the simplest tool that lets you look at the readable text embedded in a binary.

You are given: `binary`.
