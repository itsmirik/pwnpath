# Under the Layers

"Ha! Now my flag is inside a picture, so you'll never get it!"

You're given an image file. Something is covering the flag on top. Peel back what's hiding it and read the flag underneath.
