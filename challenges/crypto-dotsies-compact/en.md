# Compact

Someone claims this strange row of symbols is meant to replace the Latin
alphabet - apparently it takes up much less space on the page.

The attached image (`cipher.png`) encodes a short phrase. Decode it to recover
the flag.

Flag format: `byuctf{word or phrase}`
