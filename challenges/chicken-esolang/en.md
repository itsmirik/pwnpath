Someone tried learning a new "sorta-language" and isn't sure they got it right. The attached program consists of nothing but the word `chicken` repeated a different number of times on each line.

This is a real (if silly) esoteric language. The count of `chicken` words on each line is an opcode for a tiny stack machine that builds the flag one character at a time. Figure out the opcode meanings, run the program by hand or in a script, and read out the flag.

Note: the author admits they may have "gotten it wrong", so one opcode behaves differently than the reference spec.

You are given: `chicken.chn`.
