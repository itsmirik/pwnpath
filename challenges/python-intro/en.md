A short Python script asks you for a flag and prints `Success!` only when you type the right one.

The script never stores the flag as text. Instead it keeps a list of numbers and does a little arithmetic on each character you enter before comparing. Read the code, work out the transformation, and reverse it to recover the flag.

You are given: `pythonintro.py`.
