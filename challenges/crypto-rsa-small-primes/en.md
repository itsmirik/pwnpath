# RSA: Too Small

An RSA-encrypted message was captured. The public modulus `n`, public exponent
`e`, and ciphertext `c` are provided in `rsa.txt`.

The person who generated these keys wasn't very careful about the size of the
primes they chose. Recover the plaintext to get the flag.

Flag format: `byuctf{...}`
